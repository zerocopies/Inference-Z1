# Session Saved — ZeroCopies Tauri App

## Built Artifacts
- **Debian package**: `/home/prp/prubbs/Inference-Z1/target/release/bundle/deb/ZeroCopies_0.1.0_amd64.deb`
- **Binary**: `/home/prp/prubbs/Inference-Z1/target/release/zerocopies` (14.4MB, rebuilt 18:15)
- **Frontend dist**: `/home/prp/prubbs/Inference-Z1/ZeroCopies/dist/`

## Changes Applied
1. **Dark grey theme** — body #1F1F1F, header #2D2D2D, chat area #2D2D2D, input #374151
2. **Icons** — All app icons regenerated from `public/logo.png` (from gen.png), circular-clipped
3. **Logo display** — `clip-path:circle(50%)` on logo img, fills container
4. **Fonts** — ZeroCopies & Z1 use Space Grotesk (bold, techy); Inter for body
5. **Tagline** — Changed to `#9CA3AF` (visible light grey)
6. **Color palette** — Only approved colors used: purple (#5B21B6), dreamy pink (#FBCFE8), lavender (#E9D5FF), green (#22C55E), yellow (#FBBF24), celestial blue (#60A5FA), grey (#9CA3AF, #374151), white (#FFFFFF). No orange except online glow.
7. **State management** — Store class in `src/state.ts` with subscribe/emit pattern
8. **Layout** — Header (ZeroCopies + tagline left, inference Z1 + logo right), chat area, input zone (white/dark pill, green send, red stop, attach)

## Directory
- `/home/prp/prubbs/Inference-Z1/ZeroCopies/` — main project
- `/home/prp/prubbs/Inference-Z1/z1-core/` — backend library
- `/home/prp/prubbs/Inference-Z1/Cargo.toml` — workspace root

## Commands to Run
```bash
export PATH="$HOME/.cargo/bin:$PATH"
cd /home/prp/prubbs/Inference-Z1/ZeroCopies
npm run tauri dev    # dev mode with hot reload
npm run tauri build  # production bundle
```

## Next Steps (not done)
- Verify app opens with new logo icon in title bar/taskbar
- Test dark theme rendering across all screens
- Test online/offline ghost glow behavior
- Test pin toggle with yellow flash
- Test edit/delete on user messages
